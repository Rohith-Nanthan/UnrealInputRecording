[ContentBrowser]
ContentBrowserTab1.SelectedPaths=/Game/ThirdPersonBP
[/Script/ModelContextProtocolEngine.ModelContextProtocolSettings]
; Brings the editor's MCP bridge up on http://127.0.0.1:8000/mcp with the editor, instead of
; needing "ModelContextProtocol.StartServer" typed into the console every session. Loopback only.
bAutoStartServer=True
ServerPortNumber=8000
ServerUrlPath=/mcp
